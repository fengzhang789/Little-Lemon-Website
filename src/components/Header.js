import logo from '../assets/img/logo.svg'

const Header = () => {
    return (
        <header>
            <img src={logo} alt='little lemon logo' />
        </header>
    )
}

export default Header;